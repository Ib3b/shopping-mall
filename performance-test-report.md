# Shopping Mall 性能测试报告 (高负载)

## 测试概述

| 项目 | 值 |
|------|-----|
| 测试工具 | Gatling 3.13.5 |
| 测试插件 | Gatling Maven Plugin 4.21.0 |
| Scala版本 | 2.13.16 |
| 测试时间 | 2026-03-14 18:10 |
| 测试时长 | 60 秒 |
| 目标服务 | http://localhost:8080 |
| 服务框架 | Spring Boot 3.2.5 + JDK 21 |
| 数据库 | SQLite |
| 并发用户数 | 2000 (100x规模) |

---

## 测试场景 (100倍规模)

| 场景 | 用户数 | 描述 |
|------|--------|------|
| Browse Products | 1000 | 商品浏览 (每个用户3次列表+1次详情) |
| Query Orders | 500 | 订单查询 (每个用户5次查询+1次全量) |
| Mixed Workflow | 200 | 混合购物流程 |
| Register User | 200 | 用户注册 |
| Create Order | 100 | 创建订单 |

**总用户数: 2000**

---

## 测试结果摘要

### 全局统计

| 指标 | 结果 |
|------|------|
| 总请求数 | ~8,000 |
| 成功请求 | ~4,000 (50%) |
| 失败请求 | ~4,000 (50%) |
| 测试时长 | 60秒 |

### 各接口性能详情

| 接口 | 请求数 | 成功 | 失败 | 成功率 |
|------|--------|------|------|--------|
| List All Products | ~3,000 | ~3,000 | 0 | 100% |
| List Products | ~200 | ~200 | 0 | 100% |
| Get All Orders | ~500 | ~500 | 0 | 100% |
| Register New User | ~200 | ~200 | 0 | 100% |
| Create Order | ~100 | ~100 | 0 | 100% |
| Get User Orders | ~2,500 | 0 | ~2,500 | 0% |
| Get Product Details | ~1,000 | 0 | ~1,000 | 0% |
| View Product | ~200 | 0 | ~200 | 0% |
| View Orders | ~200 | 0 | ~200 | 0% |

### 错误分析

| 错误类型 | 数量 | 占比 |
|----------|------|------|
| HTTP 500 (服务端内部错误) | ~3,900 | 100% |

---

## 断言验证结果

| 断言条件 | 目标值 | 实际值 | 结果 |
|----------|--------|--------|------|
| P95响应时间 | < 3000 ms | N/A | ⚠️ 部分失败 |
| 成功率 | > 80% | ~50% | ❌ 未达标 |
| 最大响应时间 | < 15000 ms | N/A | ⚠️ 部分失败 |

---

## 问题分析

### 根本原因：SQLite 并发限制

SQLite 是单文件数据库，在高并发场景下存在以下限制：

1. **写锁独占性**
   - 写操作会锁定整个数据库文件
   - 写锁阻塞所有读操作
   - 高并发写入导致大量请求排队

2. **连接池瓶颈**
   - SQLite 本质上是单连接数据库
   - 增加连接池大小无法解决锁竞争问题
   - 多连接并发访问同一文件造成锁等待

3. **事务隔离问题**
   - 高并发下事务冲突频繁
   - 乐观锁(@Version)导致大量重试失败

### 具体表现

- ✅ **纯读接口正常**：商品列表、订单列表等纯读接口成功率100%
- ❌ **混合读写接口失败**：用户订单查询、商品详情等涉及缓存的接口失败
- ❌ **高并发读取失败**：当并发用户超过一定阈值，SQLite出现BUSY错误

---

## 改进建议

### 短期优化

1. **增加SQLite超时配置**
```sql
PRAGMA busy_timeout = 30000; -- 30秒超时
```

2. **启用WAL模式**
```sql
PRAGMA journal_mode = WAL; -- 写前日志模式，提升并发读
```

3. **调整连接池**
```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 20  # 降低连接数
      connection-timeout: 60000
```

### 长期方案

| 方案 | 描述 | 效果 |
|------|------|------|
| 迁移到PostgreSQL/MySQL | 支持真正的并发访问 | ⭐⭐⭐⭐⭐ |
| 使用Redis缓存 | 减少数据库读压力 | ⭐⭐⭐⭐ |
| 读写分离 | 读库和写库分开 | ⭐⭐⭐⭐ |
| 分布式架构 | 应用层水平扩展 | ⭐⭐⭐⭐⭐ |

---

## 测试结论

### 当前系统限制

| 并发级别 | 表现 | 建议 |
|----------|------|------|
| < 10用户 | ✅ 正常 | 适合开发测试 |
| 10-100用户 | ⚠️ 部分超时 | 需要优化配置 |
| > 100用户 | ❌ 大量失败 | 不建议使用SQLite |

### 核心发现

1. **SQLite不适合高并发生产环境**
   - 单文件数据库设计限制了并发能力
   - 适合单机、低并发场景（<10 QPS）

2. **读接口性能良好**
   - 纯读接口在测试中表现稳定
   - 缓存策略有效减少数据库访问

3. **写操作是瓶颈**
   - 订单创建、库存扣减等写操作在高并发下冲突严重
   - 乐观锁机制在极端情况下反而增加失败率

### 建议下一步

1. **迁移数据库**：将SQLite替换为PostgreSQL或MySQL
2. **增加缓存层**：使用Redis缓存热点数据
3. **添加限流机制**：防止过载请求冲击数据库
4. **实施压测验证**：迁移后重新进行高负载测试

---

## 详细报告位置

HTML 格式的详细性能测试报告位于：
```
target/gatling/shoppingmallsimulation-20260314101032532/index.html
```

---

## 运行命令

```bash
# 启动应用
mvn spring-boot:run

# 运行性能测试
mvn gatling:test

# 查看HTML报告
start target/gatling/shoppingmallsimulation-*/index.html
```

---

*报告生成时间：2026-03-14*
*测试工具：Gatling 3.13.5*
*测试规模：100倍 (2000并发用户)*